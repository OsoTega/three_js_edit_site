
const app = 'relative w-full h-screen overflow-hidden';

const home = 'w-fit xl:h-full flex xl:justify-between justify-start items-start flex-col xl:py-8 xl:px-36 sm:p-8 p-6 max-xl: gap-7 absolute z-10';

const home_content = 'flex-1 xl:justify-center justify-start flex flex-col gap-10';

const head_text = 'xl:text-[10rem] text-[6rem] xl:leading-[11rem] leading-[7rem] font-black text-black';

const download_btn = 'w-14 h-14 flex justify-center items-center rounded-full glassmorphism cursor-pointer outline-none';

const editortabs_container = 'glassmorphism w-16 border-[2px] rounded-lg flex flex-col justify-center items-center ml-1 py-4 gap-4';

const filtertabs_container = 'absolute z-10 bottom-5 right-0 left-0 w-full flex justify-center items-center flex-wrap gap-4';

const aipicker_container = 'absolute left-full ml-3 glassmorphism p-3 w-[195px] h-[220px] rounded-md flex flex-col gap-4';

const aipicker_textarea = 'w-full bg-transparent text-sm border border-gray-300 p-2 outline-none flex-1';

const filepicker_container = 'absolute left-full ml-3 glassmorphism p-3 w-[195px] h-[220px] flex flex-col rounded-md';

const filepicker_label = 'border border-gray-300 py-1.5 px-2 rounded-md shadow-sm text-xs text-gray-700 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-500 cursor-pointer w-fit';

const tab_btn = 'w-14 h-14 flex justify-center items-center cursor-pointer select-none';

export {
    app,
    home,
    home_content,
    head_text,
    download_btn,
    editortabs_container,
    filtertabs_container,
    aipicker_container,
    aipicker_textarea,
    filepicker_container,
    filepicker_label,
    tab_btn
};